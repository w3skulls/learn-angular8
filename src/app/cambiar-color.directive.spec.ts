import { CambiarColorDirective } from './cambiar-color.directive';

describe('CambiarColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarColorDirective();
    expect(directive).toBeTruthy();
  });
});
